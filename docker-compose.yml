services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: kowope
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - kowope_db:/var/lib/postgresql/data

volumes:
  kowope_db:
